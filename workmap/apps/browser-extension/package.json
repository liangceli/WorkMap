{
  "name": "@workmap/browser-extension",
  "version": "0.5.5",
  "private": true,
  "scripts": {
    "dev": "tsc --watch --noEmit",
    "build": "tsc && node scripts/build-alpha.mjs",
    "release:zip": "pnpm run build && node scripts/package-alpha.mjs",
    "lint": "eslint .",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "eslint": "^9.17.0"
  }
}
