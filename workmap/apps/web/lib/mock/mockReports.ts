// Frontend-only aggregated report demo data. Rows intentionally contain app
// names and domains only, never full URLs or private content.
export { reportMetrics, reportRows, type ReportMetric, type ReportRow } from "../../components/reports/mockReportsData";
