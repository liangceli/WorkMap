# Modern Office Examples

Use this folder for Modern Office example images, sample maps, and layout references.

These examples are only layout references. Do not use them directly as the official WorkMap production map.

The final WorkMap map should be created by WorkMap in Tiled as:

```text
workmap_office_mvp.json
```
